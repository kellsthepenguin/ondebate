import TopBar from './TopBar'
import { Socket } from 'socket.io-client'

export default function DebatePage({ socket }: { socket: Socket }) {
  return (
    <div>
      <TopBar />
      TODO
    </div>
  )
}
